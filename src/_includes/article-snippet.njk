<!-- <li> -->
	{# <a href="{{ post.url }}" class="gallery-item"> #}
        <article class="card" onclick="window.location.href='{{ post.url }}'">
            <img class="card-img" src="{{ post.data.image }}" alt="{{ post.data.imageAlt }}">
            <div class="card-text">
                <h3>{{ post.data.title }}</h3>
                <p>{{ post.data.description }}</p>
            </div>
        </article>
        {# </a> #}
<!--</li>-->